import facebookStrategy from '../src/authentication/strategies/strategy.facebook';
import sinon from 'sinon';
import { getEnabled } from '../src/config/functions/getEnabled';
import facebookStrategyMongodb from '../src/authentication/strategies/strategy.facebook.mongodb';

describe('strategy.facebook.ts', () => {
  it.skip('will correctly call the MongoDb Callback if Mongodb is enabled', () => {});
});
